{% comment %}
  Renders loading-spinner.
  Accepts:
    - class: {string} css classes to replace the default ones (optional)

  Usage:
  {% render 'loading-overlay' %}
{% endcomment %}

<div class="{% if class %}{{ class }}{% else %}loading-overlay spinner hidden{% endif %}">
 {% render 'icon', icon: 'spinner' %}
</div>